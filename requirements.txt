fastapi
uvicorn

# Database
sqlite3-binary   # (sqlite3 is built-in with Python, but some deploys want a stub)

# QR code generation
qrcode
Pillow          # imaging backend for qrcode

# PDF generation
reportlab

# Email + SendGrid
sendgrid
python-dotenv

# HTTP requests for geocoding
requests