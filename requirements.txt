fastapi==0.115.0
uvicorn==0.32.0

# Database
sqlite3-binary==0.0.1   # (sqlite3 is built-in with Python, but some deploys want a stub)

# QR code generation
qrcode==7.4.2
Pillow==10.4.0           # imaging backend for qrcode

# PDF generation
reportlab==4.2.5

# Email + SendGrid
sendgrid==6.11.0
python-dotenv==1.0.1

# HTTP requests for geocoding
requests==2.32.3